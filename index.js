class Driver {
  constructor(name, date) {
    this.name = name
    this.date = date
  }
  
  startDate() {
    const startDate = new Date(this.date)
  }
  
  yearsExperienceFromBeginningOf(year) {
    
  }
}
